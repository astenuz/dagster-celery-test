execution:
  celery:
    config:
      broker: 'pyamqp://rabbitmq:rabbitmq@rabbitmq:5672/dagster'  # Optional[str]: The URL of the Celery broker
storage:
  s3:
    config:
      s3_bucket: dgamba-dagster-test
